name: Self Hosted Pull Request
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  test:
    runs-on: [self-hosted, linux]
    timeout-minutes: 10
    steps:
      - run: echo ok
