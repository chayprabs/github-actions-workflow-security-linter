name: Secrets In Run
on:
  workflow_dispatch:
permissions:
  contents: read
env:
  RELEASE_TOKEN: ${{ secrets.RELEASE_TOKEN }}
jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - run: echo "token is available at runtime via env"
